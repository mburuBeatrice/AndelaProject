def find_max_min(x):
    if min(x) == max(x):
      return [len(x)]
    else:
      return [min(x), max(x)]
def find_max_min(x):
    if min(x) == max(x):
      return [len(x)]
    else:
      return [min(x), max(x)]